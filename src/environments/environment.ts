// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: "AIzaSyCBIaa1Uvyf5d_PF38aLEc4-pNOttq8o2s",
    authDomain: "book-management-tool.firebaseapp.com",
    projectId: "book-management-tool",
    storageBucket: "book-management-tool.appspot.com",
    messagingSenderId: "473621341120",
    appId: "1:473621341120:web:d38e51d639d57cd4d15e25"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
